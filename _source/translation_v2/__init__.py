"""Localization contracts, accepted content, and explicit V2 generation.

Import the module needed by the caller; reading accepted content must not import
model runners, evaluation fixtures, or generation workflows.
"""
